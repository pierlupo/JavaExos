package org.example.exercices.exo2;

import java.util.Arrays;

public class ContactService {

//    public Contact[] filterContactsByName(Contact contact, Contact[] contacts) {
//        if (contact == null || contacts == null) {
//            return new Contact[0];
//        }

//        Contact[] filteredContacts = Arrays.stream(contacts)
//                .filter(contact -> Arrays.stream(contact.getLastName().anyMatch(cont -> cont.equals(contact)))
//                .toArray(Contact[]::new);
//
//        return filteredContacts;
}
